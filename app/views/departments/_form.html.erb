<%= form_for([@factory, @department]) do |f| %>
  <% if @department.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@department.errors.count, "error") %> prohibited this department from being saved:</h2>

      <ul>
      <% @department.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :department_number %><br />
    <%= f.number_field :department_number %>
  </div>
  <%= hidden_field :department, :factory_id, :value => @factory.id %>
<!--  <div class="field">
    <%= f.label :factory_id %><br />
    <%= f.number_field :factory_id %>
  </div>-->
  <div class="field">
    <%= f.label :chief_name %><br />
    <%= f.text_field :chief_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
